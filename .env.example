# Copy to .env and fill in. .env is gitignored.

# Hugging Face token — needed for `hf download` rate limits and for pushing the
# fine-tuned model / Space (Phase 4). Get one at https://huggingface.co/settings/tokens
HF_TOKEN=

# Optional: redirect the project's JSONL logs/manifest root (§17). Defaults to
# the repo root if unset.
# EXAONE_YAHO_ROOT=/Users/you/Desktop/Programming/exaone_yaho
