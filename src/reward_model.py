"""§2 / §7.6 — Reward model (STRETCH goal, for online RL only).

Optional: a style/appropriateness reward model to enable online RL (PPO/GRPO).
Out of scope for the MVP and even for Phase 2.5 (which uses ORPO/DPO directly).
Kept as a placeholder so the architecture (§4) shows the full alignment arc.
"""

from __future__ import annotations


def train_reward_model(*args, **kwargs):  # pragma: no cover
    raise NotImplementedError("Stretch goal — only if pursuing online RL (§7.6)")
